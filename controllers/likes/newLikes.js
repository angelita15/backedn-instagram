const insertLikeQuery = require('../../db/likesQueries/insertLikeQuery')

const newLike = async (req, res, next) => {
    try {
        const { like }= req.body;

        if (!like ) {
            
        }
    }
}